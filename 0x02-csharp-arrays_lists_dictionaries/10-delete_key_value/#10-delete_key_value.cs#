using System;
using System.Collections.Generic;

class Dictionary
{
    public static Dictionary<string, string> DeleteKeyValue(Dictionary<string, string> dict, string key)
    {
        if (dict == null)
        {
            return dict;
        }

        dict.Remove(key);

        return myDict;
    }
}
